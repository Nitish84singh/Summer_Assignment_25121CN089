#include <stdio.h>
int main() {
    int n,d,sum=0,temp;
    printf("Enter the number:");
    scanf("%d",&n);
    temp=n;
    while(n>0){
        d=n%10;
        sum=sum+d;
        n=n/10;
    }
    printf("sum of the digit are %d",sum);    
    return 0;
}
